{-
Try out the Rounding library. https://www.stackage.org/package/rounded

Can't get it to install. Intstalled mpfr but stack is not finding it. Look in ~\mpfr\version.c which when run, shows that mpfr ins installed, but perhaps on in a standard location.
-}

module Rounding() where

